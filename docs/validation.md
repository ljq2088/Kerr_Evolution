# 数值验证记录

2026-09-10，在 WSL 独立环境运行。

`python -m unittest discover -s tests -v`：8 项测试全部通过。
包括径向多项式微分、角向独立求积、Ripley (2022) 式 (11) 的系数、
端点特征方向、Schwarzschild 无模态耦合、Kerr 共轭/奇偶及模态耦合、质量缩放和非法参数拒绝。

`python src/validate.py`：所有径向、角向及时间收敛断言通过。
比较初值 `l=2`、`M=1,a/M=.7`、`tau=0..80`，两端点、所有共同模态、201 个共同时间样本的最大绝对差。
这些是不同离散结果间的差，不是与精确解比较的绝对误差上界。

| 检查 | m=0 | m=2 |
|---|---:|---:|
| 径向 N=32 与 48 | 2.0944e-6 | 2.0705e-6 |
| 径向 N=48 与 64 | 2.4624e-9 | 9.8697e-10 |
| 角向 lmax=4 与 6 | 1.1609e-6 | 5.8342e-7 |
| 角向 lmax=6 与 8 | 1.5468e-8 | 7.8821e-9 |
| 时间容差收紧后的差 | 3.2725e-9 | 1.6775e-9 |
| 两端波形峰值 | .34937 | .33740 |

径向测试固定 `lmax=6`；角向和时间测试固定 `N=64`。
精细时间容差为 `rtol=2e-11,atol=2e-13`；与默认 `1e-9,1e-11` 比较。
两个非零旋转示例均演化至 `80M` 并输出完整场数据、端点波形及参数记录。
详细机器可读结果：`outputs/validation/convergence.json`。

限制：尚未验证极长时尾波指数、近极端 Kerr 的长时间稳定性、QNM 频率和能量/角动量守恒。
以上结果支持当前示例及该精度范围内的时域演化，不应外推为任意初值或参数的误差保证。
